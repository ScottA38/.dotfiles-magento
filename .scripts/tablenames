#!/usr/bin/env bash

SCHEMA=$1;
# 'TABLE' argument should be in sql string pattern matching format
TABLE=$2;

mysql -BAe "SELECT TABLE_NAME FROM tables WHERE TABLE_SCHEMA = '$SCHEMA' AND TABLE_NAME LIKE '$TABLE';" information_schema | awk 'FNR > 1'
